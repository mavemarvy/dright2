import { useEffect, useMemo, useState } from 'react';
import { useLocation, useSearchParams } from 'react-router-dom';
import { motion } from 'framer-motion';
import { AlertTriangle, AtSign, Bell, ExternalLink, Loader2, Megaphone, Pin, Share2, ShoppingBag, Tag, TrendingUp, Users, type LucideIcon } from 'lucide-react';
import SeoHead from '../components/SeoHead';
import SponsoredPlacementCard from '../components/promotion/SponsoredPlacementCard';
import { supabase } from '../lib/supabase';
import NewsPage from './NewsPage';
import SocialFieldPage from './SocialFieldPage';

type AnnouncementCategory='announcement'|'update'|'market'|'affiliate'|'promo'|'referral'|'news';
type Filter='all'|Exclude<AnnouncementCategory,'news'>; type SortOption='newest'|'oldest'|'trending';
interface Announcement{id:string;title:string;message:string;type:AnnouncementCategory;content_kind:'announcement'|'news';show_in_news:boolean;is_active:boolean;is_pinned:boolean;is_trending:boolean;mention_all:boolean;view_count:number;media_url:string|null;media_type:'image'|'video'|null;external_url:string|null;cta_label:string|null;created_at:string;published_at:string;updated_at:string;}
const FILTERS:{value:Filter;label:string}[]=[{value:'all',label:'All'},{value:'announcement',label:'Announcements'},{value:'update',label:'Updates'},{value:'market',label:'Market'},{value:'affiliate',label:'Affiliate'},{value:'promo',label:'Promo'},{value:'referral',label:'Referral'}];
const CONFIG:Record<Exclude<AnnouncementCategory,'news'>,{icon:LucideIcon;label:string;color:string;bg:string}>={announcement:{icon:Megaphone,label:'Announcement',color:'text-slate-700 dark:text-slate-200',bg:'bg-slate-100 dark:bg-slate-700'},update:{icon:AlertTriangle,label:'Update',color:'text-amber-700 dark:text-amber-300',bg:'bg-amber-50 dark:bg-amber-900/20'},market:{icon:ShoppingBag,label:'Market',color:'text-cyan-700 dark:text-cyan-300',bg:'bg-cyan-50 dark:bg-cyan-900/20'},affiliate:{icon:Users,label:'Affiliate',color:'text-violet-700 dark:text-violet-300',bg:'bg-violet-50 dark:bg-violet-900/20'},promo:{icon:Tag,label:'Promo',color:'text-emerald-700 dark:text-emerald-300',bg:'bg-emerald-50 dark:bg-emerald-900/20'},referral:{icon:Share2,label:'Referral',color:'text-pink-700 dark:text-pink-300',bg:'bg-pink-50 dark:bg-pink-900/20'}};
function normalizedCategory(type:AnnouncementCategory):Exclude<AnnouncementCategory,'news'>{return type==='news'?'announcement':type;}

function AnnouncementsContent(){
 const [searchParams,setSearchParams]=useSearchParams(); const targetItemId=searchParams.get('item'); const [announcements,setAnnouncements]=useState<Announcement[]>([]); const [loading,setLoading]=useState(true); const [filter,setFilter]=useState<Filter>('all'); const [sort,setSort]=useState<SortOption>('newest');
 useEffect(()=>{let active=true;void(async()=>{setLoading(true);const {data}=await supabase.from('global_announcements').select('id,title,message,type,content_kind,show_in_news,is_active,is_pinned,is_trending,mention_all,view_count,media_url,media_type,external_url,cta_label,created_at,published_at,updated_at').eq('is_active',true).eq('content_kind','announcement').order('published_at',{ascending:false});if(active){setAnnouncements((data||[]) as Announcement[]);setLoading(false);}})();return()=>{active=false};},[]);
 useEffect(()=>{if(!targetItemId||loading||!announcements.some(x=>x.id===targetItemId))return;document.getElementById(`announcement-${targetItemId}`)?.scrollIntoView({behavior:'smooth',block:'center'});},[announcements,loading,targetItemId]);
 const filtered=useMemo(()=>{const list=filter==='all'?announcements:announcements.filter(a=>normalizedCategory(a.type)===filter);return [...list].sort((a,b)=>{if(a.is_pinned!==b.is_pinned)return a.is_pinned?-1:1;if(sort==='oldest')return +new Date(a.published_at)-+new Date(b.published_at);if(sort==='trending'){if(a.is_trending!==b.is_trending)return a.is_trending?-1:1;if(b.view_count!==a.view_count)return b.view_count-a.view_count;}return +new Date(b.published_at)-+new Date(a.published_at);});},[announcements,filter,sort]);
 return <div className="min-h-screen bg-gray-50 dark:bg-gray-900"><SeoHead title="Announcements" description="Official DRIGHT announcements, platform updates, promotions, marketplace, affiliate, and referral notices." canonical="/announcements"/><div className="bg-gradient-to-br from-blue-600 to-indigo-700 px-4 py-12 text-white sm:py-14"><div className="mx-auto max-w-4xl text-center"><Megaphone className="mx-auto mb-4 h-12 w-12 opacity-80"/><h1 className="mb-3 text-3xl font-bold sm:text-4xl">Announcements</h1><p className="text-blue-100">Official DRIGHT announcements and important platform notices.</p></div></div><div className="mx-auto max-w-4xl px-4 py-8 sm:py-10"><div className="mb-6 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div className="flex gap-2 overflow-x-auto pb-1">{FILTERS.map(x=><button key={x.value} type="button" onClick={()=>setFilter(x.value)} className={`shrink-0 rounded-full px-4 py-2 text-sm font-medium ${filter===x.value?'bg-blue-600 text-white':'border border-gray-200 bg-white text-gray-600 dark:border-gray-700 dark:bg-gray-800 dark:text-gray-300'}`}>{x.label}</button>)}</div><select value={sort} onChange={e=>setSort(e.target.value as SortOption)} className="min-h-[42px] rounded-xl border border-gray-200 bg-white px-3 text-sm dark:border-gray-700 dark:bg-gray-800 dark:text-gray-200"><option value="newest">Recently posted</option><option value="oldest">Oldest first</option><option value="trending">Trending</option></select></div><SponsoredPlacementCard placement="announcement_banner" variant="compact" className="mb-6" heading="Sponsored announcement"/>{loading?<div className="flex justify-center py-12"><Loader2 className="h-6 w-6 animate-spin text-blue-500"/></div>:filtered.length===0?<div className="py-14 text-center text-gray-400"><Bell className="mx-auto mb-3 h-12 w-12 opacity-30"/><p>No announcements available.</p></div>:<div className="space-y-5">{filtered.map((a,index)=>{const config=CONFIG[normalizedCategory(a.type)];const Icon=config.icon;const targeted=targetItemId===a.id;return <motion.article id={`announcement-${a.id}`} key={a.id} initial={{opacity:0,y:10}} animate={{opacity:1,y:0}} transition={{delay:Math.min(index*.04,.2)}} className={`overflow-hidden rounded-2xl border bg-white shadow-sm dark:bg-gray-800 ${targeted?'border-blue-500 ring-2 ring-blue-500/20':a.is_pinned?'border-blue-200 dark:border-blue-800':'border-gray-100 dark:border-gray-700'}`}>{a.media_url&&<div className="max-h-[520px] overflow-hidden bg-black">{a.media_type==='video'?<video src={a.media_url} controls playsInline preload="metadata" className="max-h-[520px] w-full object-contain"/>:<img src={a.media_url} alt="" loading="lazy" className="max-h-[520px] w-full object-cover"/>}</div>}<div className="p-5"><div className="flex items-start gap-3"><div className={`flex h-10 w-10 shrink-0 items-center justify-center rounded-xl ${config.bg}`}><Icon className={`h-5 w-5 ${config.color}`}/></div><div className="min-w-0 flex-1"><div className="mb-1 flex flex-wrap items-center gap-2"><span className={`rounded-full px-2 py-.5 text-xs font-medium uppercase ${config.bg} ${config.color}`}>{config.label}</span>{a.is_pinned&&<span className="inline-flex items-center gap-1 text-[10px] text-blue-700"><Pin className="h-3 w-3"/>Pinned</span>}{a.is_trending&&<span className="inline-flex items-center gap-1 text-[10px] text-orange-700"><TrendingUp className="h-3 w-3"/>Trending</span>}{a.mention_all&&<span className="inline-flex items-center gap-1 text-[10px] text-violet-700"><AtSign className="h-3 w-3"/>All DRIGHT</span>}<span className="text-xs text-gray-400">{new Date(a.published_at).toLocaleDateString()}</span></div><button type="button" onClick={()=>setSearchParams({item:a.id})} className="text-left"><h2 className="text-lg font-semibold text-gray-900 hover:text-blue-600 dark:text-white">{a.title}</h2></button><p className={`mt-2 whitespace-pre-wrap text-sm leading-relaxed text-gray-600 dark:text-gray-300 ${targeted?'':'line-clamp-2'}`}>{a.message}</p><div className="mt-4 flex flex-wrap gap-2">{a.external_url&&<a href={a.external_url} target="_blank" rel="noreferrer" className="inline-flex min-h-[38px] items-center gap-2 rounded-xl bg-blue-600 px-4 py-2 text-xs font-semibold text-white">{a.cta_label||'Learn more'}<ExternalLink className="h-3.5 w-3.5"/></a>}{a.show_in_news&&<a href={`/news?item=${a.id}`} className="inline-flex min-h-[38px] items-center gap-2 rounded-xl border border-gray-200 px-4 py-2 text-xs dark:border-gray-600">View in News<ExternalLink className="h-3.5 w-3.5"/></a>}</div></div></div></div></motion.article>;})}</div>}</div></div>;
}

export default function AnnouncementsPage(){const location=useLocation();const params=new URLSearchParams(location.search);if(location.pathname==='/news'&&params.get('social')==='1')return <SocialFieldPage/>;return location.pathname==='/news'?<NewsPage/>:<AnnouncementsContent/>;}
