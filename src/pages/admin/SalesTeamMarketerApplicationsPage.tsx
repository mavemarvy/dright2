import { useCallback, useEffect, useState } from 'react';
import { CheckCircle, ExternalLink, RefreshCw, Search, ShieldAlert, XCircle, Bot, ScanSearch, Microscope } from 'lucide-react';
import { supabase } from '../../lib/supabase';

type Analysis={profile_url:string;final_url?:string;canonical_url?:string|null;platform:string;status:string;profile_title?:string|null;profile_description?:string|null;followers?:number|null;following?:number|null;likes?:number|null;content_count?:number|null;verified_indicator?:boolean;authenticity_score?:number|null;risk_level?:'LOW'|'MEDIUM'|'HIGH'|'UNKNOWN';confidence_score?:number;signals?:string[];error?:string;disclaimer?:string;analyzed_at?:string};
type DeepResearch={profile_url:string;platform:string;status:string;provider?:string;model?:string|null;research:any;search_evidence?:{title:string;link:string;snippet:string;source:string;query:string}[];search_queries?:string[];requested_at:string};
type Application={id:string;email:string;full_name:string|null;username:string|null;avatar_url:string|null;created_at:string;joined_at:string|null;location:string|null;profession:string|null;marketer_level:number;marketer_status:string;social_media_links:unknown;marketer_social_analysis:Analysis[];marketer_deep_research?:DeepResearch[];verification_status:string|null;is_verified:boolean;last_active_at:string|null};
type Review={id:string;applicant_id:string;reviewer_id:string;decision:string;note:string|null;social_links_snapshot:unknown;created_at:string};

const linksOf=(value:unknown):string[]=>Array.isArray(value)?value.map(v=>String(v)).filter(Boolean):[];
const safeFunctionError=(error:unknown,data:any,fallback:string)=>{if(data?.error){if(typeof data.error==='string')return new Error(data.error);if(data.error.message)return new Error(`${data.error.code?`${data.error.code}: `:''}${data.error.message}`)}if(error instanceof Error)return error;return new Error(fallback)};
const reviewCall=async(method:'GET'|'POST',body?:Record<string,unknown>)=>{const {data,error}=await supabase.functions.invoke('sales-team-review',{method,body});if(error||data?.error)throw safeFunctionError(error,data,'Sales Team request failed');return data};
const analyzerCall=async(applicantId:string)=>{const {data,error}=await supabase.functions.invoke('social-profile-analyzer',{method:'POST',body:{applicant_id:applicantId}});if(error||data?.error)throw safeFunctionError(error,data,'Automatic profile analysis failed');return data};
const deepResearchCall=async(applicantId:string,profileUrl?:string)=>{const {data,error}=await supabase.functions.invoke('marketer-profile-deep-research',{method:'POST',body:{applicant_id:applicantId,profile_url:profileUrl}});if(error||data?.error)throw safeFunctionError(error,data,'Deep Research failed');return data};
const count=(n:number|null|undefined)=>n==null?'UNKNOWN':n>=1e9?`${(n/1e9).toFixed(1)}B`:n>=1e6?`${(n/1e6).toFixed(1)}M`:n>=1e3?`${(n/1e3).toFixed(1)}K`:n.toLocaleString();

export default function SalesTeamMarketerApplicationsPage(){
 const[apps,setApps]=useState<Application[]>([]);const[history,setHistory]=useState<Review[]>([]);const[selected,setSelected]=useState<Application|null>(null);const[loading,setLoading]=useState(true);const[busy,setBusy]=useState('');const[analysisBusy,setAnalysisBusy]=useState(false);const[deepResearchBusy,setDeepResearchBusy]=useState(false);const[deepStage,setDeepStage]=useState('');const[query,setQuery]=useState('');const[note,setNote]=useState('');const[error,setError]=useState('');
 const load=useCallback(async()=>{setLoading(true);setError('');try{const d=await reviewCall('GET');const pending=d.pendingApplications||[];setApps(pending);setHistory(d.reviewHistory||[]);setSelected(s=>s?pending.find((a:Application)=>a.id===s.id)||null:null)}catch(e){setError(e instanceof Error?e.message:'Failed to load marketer applications')}finally{setLoading(false)}},[]);
 useEffect(()=>{load()},[load]);
 const reanalyze=async()=>{if(!selected)return;setAnalysisBusy(true);setError('');try{await analyzerCall(selected.id);await load()}catch(e){setError(e instanceof Error?e.message:'Automatic profile analysis failed')}finally{setAnalysisBusy(false)}};
 const deepResearch=async()=>{if(!selected)return;setDeepResearchBusy(true);setDeepStage('Searching public sources…');setError('');try{const promise=deepResearchCall(selected.id);const timer=window.setTimeout(()=>setDeepStage('Analyzing evidence with DRIGHT AI…'),1200);const data=await promise;window.clearTimeout(timer);if(!data?.success){const first=data?.errors?.find((x:any)=>x?.stage==='ai'||x?.stage==='search'||x?.stage==='database');throw new Error(first?.error?.message?`${first.error.code}: ${first.error.message}`:'Deep Research did not complete.')}setDeepStage('Deep Research completed.');await load()}catch(e){setDeepStage('');setError(e instanceof Error?e.message:'Deep Research failed')}finally{setDeepResearchBusy(false)}};
 const review=async(decision:'approved'|'rejected'|'needs_changes')=>{if(!selected)return;setBusy(decision);setError('');try{await reviewCall('POST',{action:'review_marketer_application',applicant_id:selected.id,decision,note});setNote('');setSelected(null);await load()}catch(e){setError(e instanceof Error?e.message:'Review failed')}finally{setBusy('')}};
 const filtered=apps.filter(a=>{const q=query.trim().toLowerCase();return !q||[a.full_name,a.email,a.username,a.profession,a.location].filter(Boolean).some(v=>String(v).toLowerCase().includes(q))});
 return <div className="p-4 md:p-8 max-w-7xl mx-auto"><div className="flex flex-col md:flex-row md:items-center md:justify-between gap-4 mb-6"><div><h1 className="text-2xl font-bold text-gray-900">Marketer Applications</h1><p className="text-sm text-gray-500">Review users who submitted social or professional profile links to join the Marketer program.</p></div><button onClick={load} disabled={loading} className="inline-flex items-center gap-2 px-3 py-2 rounded-xl border border-gray-200 text-sm disabled:opacity-50"><RefreshCw className="w-4 h-4"/>Refresh</button></div>{error&&<div className="mb-4 rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</div>}
 <div className="grid lg:grid-cols-[380px_1fr] gap-5"><section className="bg-white border border-gray-100 rounded-2xl overflow-hidden"><div className="p-4 border-b border-gray-100"><div className="flex items-center justify-between mb-3"><h2 className="font-bold">Pending review <span className="text-xs font-semibold px-2 py-1 rounded-full bg-amber-50 text-amber-700">{apps.length}</span></h2></div><div className="relative"><Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-gray-400"/><input className="input pl-9" placeholder="Search applicant" value={query} onChange={e=>setQuery(e.target.value)}/></div></div><div className="max-h-[650px] overflow-y-auto">{loading?<div className="p-8 text-center text-gray-400">Loading…</div>:filtered.length===0?<div className="p-10 text-center text-gray-400">No pending marketer applications.</div>:filtered.map(a=><button key={a.id} onClick={()=>{setSelected(a);setNote('');setError('')}} className={`w-full text-left p-4 border-b border-gray-100 hover:bg-gray-50 ${selected?.id===a.id?'bg-gray-50 border-l-4 border-l-gray-900':''}`}><div className="font-semibold">{a.full_name||a.username||a.email}</div><div className="text-xs text-gray-500 mt-1">{a.email}</div><div className="text-xs text-gray-400 mt-2">Submitted {new Date(a.created_at).toLocaleString()}</div><div className="mt-2 flex flex-wrap gap-1">{linksOf(a.social_media_links).map((l,i)=><span key={i} className="text-[11px] px-2 py-1 rounded-full bg-gray-100">{(()=>{try{return new URL(l).hostname.replace('www.','')}catch{return 'profile'}})()}</span>)}</div></button>)}</div></section>
 <section className="bg-white border border-gray-100 rounded-2xl p-5">{!selected?<div className="h-full min-h-[500px] flex flex-col items-center justify-center text-center text-gray-400"><ShieldAlert className="w-10 h-10 mb-3"/><h2 className="font-semibold text-gray-600">Select an application</h2><p className="text-sm max-w-md mt-1">Choose a pending applicant to inspect submitted profiles and run evidence-based research.</p></div>:<div><div className="flex flex-col md:flex-row md:items-start md:justify-between gap-4 border-b border-gray-100 pb-5"><div><h2 className="text-xl font-bold">{selected.full_name||selected.username||'Applicant'}</h2><p className="text-sm text-gray-500">{selected.email}</p><div className="flex flex-wrap gap-2 mt-3"><span className="text-xs px-2.5 py-1 rounded-full bg-amber-50 text-amber-700">Pending review</span><span className="text-xs px-2.5 py-1 rounded-full bg-gray-100">Marketer {selected.marketer_level}</span>{selected.is_verified&&<span className="text-xs px-2.5 py-1 rounded-full bg-green-50 text-green-700">DRIGHT verified</span>}</div></div><div className="text-xs text-gray-400">Account created {new Date(selected.created_at).toLocaleDateString()}</div></div>
 <div className="grid sm:grid-cols-2 gap-3 py-5">{selected.profession&&<Info label="Profession" value={selected.profession}/>} {selected.location&&<Info label="Location" value={selected.location}/>} {selected.verification_status&&<Info label="Verification status" value={selected.verification_status}/>} {selected.last_active_at&&<Info label="Last active" value={new Date(selected.last_active_at).toLocaleString()}/>}</div>
 <div className="mb-6 rounded-2xl border border-gray-200 bg-gray-50 p-4"><div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3 mb-4"><div><div className="flex items-center gap-2"><ScanSearch className="w-5 h-5"/><h3 className="font-bold">Automatic profile analysis</h3></div><p className="text-xs text-gray-500 mt-1">Existing DRIGHT profile analysis is displayed here. Missing platform metrics remain UNKNOWN and are never treated as zero.</p></div><div className="flex flex-wrap gap-2"><button onClick={reanalyze} disabled={analysisBusy||deepResearchBusy} className="inline-flex items-center gap-2 px-3 py-2 rounded-xl bg-white border border-gray-200 text-sm font-semibold disabled:opacity-50"><RefreshCw className={`w-4 h-4 ${analysisBusy?'animate-spin':''}`}/>{analysisBusy?'Analyzing…':'Re-analyze'}</button><button onClick={deepResearch} disabled={analysisBusy||deepResearchBusy} className="inline-flex items-center gap-2 px-3 py-2 rounded-xl bg-gray-900 text-white text-sm font-semibold disabled:opacity-50"><Microscope className={`w-4 h-4 ${deepResearchBusy?'animate-pulse':''}`}/>{deepResearchBusy?(deepStage||'Researching…'):'Deep Research with AI'}</button></div></div><div className="space-y-3">{(selected.marketer_social_analysis||[]).length===0?<div className="text-sm text-gray-500">Analysis is pending or the platform did not expose public data.</div>:(selected.marketer_social_analysis||[]).map((a,i)=><AnalysisCard key={`${a.profile_url}-${i}`} analysis={a}/>)}</div></div>
 {(selected.marketer_deep_research||[]).length>0&&<div className="mb-6 rounded-2xl border border-indigo-200 bg-indigo-50/40 p-4"><div className="flex items-center gap-2 mb-4"><Microscope className="w-5 h-5"/><div><h3 className="font-bold">DEEP RESEARCH REPORT</h3><p className="text-xs text-gray-500">Historical runs are preserved. AI assessments are estimates, not proof.</p></div></div><div className="space-y-4">{(selected.marketer_deep_research||[]).slice(0,10).map((r,i)=><DeepResearchCard key={`${r.profile_url}-${i}-${r.requested_at}`} report={r}/>)}</div></div>}
 <div><h3 className="font-bold mb-3">Submitted profiles</h3><div className="space-y-3">{linksOf(selected.social_media_links).map((link,i)=><div key={`${link}-${i}`} className="flex items-center justify-between gap-3 border border-gray-100 rounded-xl p-3"><div className="min-w-0"><div className="text-xs font-semibold text-gray-500">Profile {i+1}</div><div className="text-sm truncate mt-1">{link}</div></div><a href={link} target="_blank" rel="noreferrer" className="shrink-0 inline-flex items-center gap-1 px-3 py-2 rounded-lg bg-gray-100 text-sm font-medium">Open <ExternalLink className="w-4 h-4"/></a></div>)}</div></div>
 <div className="mt-6"><label className="block text-xs font-medium text-gray-500 mb-1.5">Review note (optional)</label><textarea className="input min-h-24" placeholder="Record why the application was approved, rejected, or returned for changes…" value={note} onChange={e=>setNote(e.target.value)}/></div><div className="mt-5 flex flex-wrap gap-2"><button onClick={()=>review('approved')} disabled={!!busy||deepResearchBusy} className="inline-flex items-center gap-2 px-4 py-2.5 rounded-xl bg-gray-900 text-white text-sm font-semibold disabled:opacity-50"><CheckCircle className="w-4 h-4"/>{busy==='approved'?'Approving…':'Approve Marketer'}</button><button onClick={()=>review('needs_changes')} disabled={!!busy||deepResearchBusy} className="px-4 py-2.5 rounded-xl border border-amber-300 bg-amber-50 text-amber-800 text-sm font-semibold disabled:opacity-50">Request Changes</button><button onClick={()=>review('rejected')} disabled={!!busy||deepResearchBusy} className="inline-flex items-center gap-2 px-4 py-2.5 rounded-xl border border-red-200 bg-red-50 text-red-700 text-sm font-semibold disabled:opacity-50"><XCircle className="w-4 h-4"/>{busy==='rejected'?'Rejecting…':'Reject'}</button></div></div>}</section></div>
 <section className="mt-5 bg-white border border-gray-100 rounded-2xl p-5"><h2 className="font-bold mb-3">Recent review history</h2>{history.length===0?<p className="text-sm text-gray-400">No reviews recorded yet.</p>:<div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b border-gray-100 text-left text-gray-500"><th className="py-2 pr-4">Date</th><th className="py-2 pr-4">Applicant</th><th className="py-2 pr-4">Decision</th><th className="py-2">Note</th></tr></thead><tbody className="divide-y divide-gray-100">{history.slice(0,50).map(h=><tr key={h.id}><td className="py-3 pr-4">{new Date(h.created_at).toLocaleString()}</td><td className="py-3 pr-4">{h.applicant_id.slice(0,8)}…</td><td className="py-3 pr-4 capitalize font-semibold">{h.decision.replace('_',' ')}</td><td className="py-3 text-gray-500">{h.note||'—'}</td></tr>)}</tbody></table></div>}</section></div>;
}
function AnalysisCard({analysis:a}:{analysis:Analysis}){const risk=a.risk_level||'UNKNOWN';const riskClass=risk==='LOW'?'bg-green-50 text-green-700 border-green-200':risk==='MEDIUM'?'bg-amber-50 text-amber-700 border-amber-200':risk==='HIGH'?'bg-red-50 text-red-700 border-red-200':'bg-gray-100 text-gray-600 border-gray-200';return <div className="rounded-xl border border-gray-200 bg-white p-4"><div className="flex flex-col md:flex-row md:items-start md:justify-between gap-3"><div className="min-w-0"><div className="flex items-center gap-2"><Bot className="w-4 h-4"/><span className="font-bold">{a.platform}</span><span className="text-xs px-2 py-0.5 rounded-full bg-gray-100">{a.status.replaceAll('_',' ')}</span></div><div className="text-sm font-medium mt-2 truncate">{a.profile_title||a.profile_url}</div>{a.profile_description&&<p className="text-xs text-gray-500 mt-1 line-clamp-2">{a.profile_description}</p>}</div><div className={`shrink-0 text-xs font-bold px-2.5 py-1.5 rounded-full border ${riskClass}`}>{risk==='UNKNOWN'?'RISK UNKNOWN':`${risk} RISK`}</div></div>{a.status==='analyzed'&&<><div className="grid grid-cols-2 md:grid-cols-5 gap-2 mt-4">{[['Followers',count(a.followers)],['Following',count(a.following)],['Likes',count(a.likes)],['Content',count(a.content_count)],['Authenticity',a.authenticity_score==null?'UNKNOWN':`${a.authenticity_score}/100`]].map(([l,v])=><div key={String(l)} className="rounded-lg bg-gray-50 p-2"><div className="text-[10px] text-gray-400">{l}</div><div className="text-sm font-bold mt-1">{v}</div></div>)}</div><div className="mt-3 flex flex-wrap gap-2 text-xs">{a.verified_indicator&&<span className="px-2 py-1 rounded-full bg-green-50 text-green-700">✓ Verification indicator</span>}<span className="px-2 py-1 rounded-full bg-gray-100">Confidence {a.confidence_score==null?'UNKNOWN':`${a.confidence_score}%`}</span></div>{a.signals?.length?<div className="mt-3 text-xs text-gray-500"><span className="font-semibold text-gray-700">Signals:</span> {a.signals.join(' • ')}</div>:null}</>:<div className="mt-3 text-sm text-gray-500">{a.error||'The platform did not expose enough public information for automated analysis. Use Open to verify manually.'}</div>}<div className="mt-3 flex justify-end"><a href={a.final_url||a.profile_url} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1 text-xs font-semibold text-gray-700">Open profile <ExternalLink className="w-3.5 h-3.5"/></a></div></div>}
function DeepResearchCard({report:r}:{report:DeepResearch}){const x=r.research||{};const score=(v:any)=>typeof v==='number'?`${v}/100`:'UNKNOWN';const confidence=(v:any)=>typeof v==='number'?`${v}%`:'UNKNOWN';const badge=(v:any)=>String(v??'unknown').replaceAll('_',' ');const items=(v:any)=>Array.isArray(v)?v:[];return <div className="rounded-xl border border-indigo-100 bg-white p-4"><div className="flex flex-col md:flex-row md:items-start md:justify-between gap-3"><div><div className="font-semibold">{r.platform} · {r.profile_url}</div><div className="text-xs text-gray-400 mt-1">Research date: {new Date(r.requested_at).toLocaleString()}</div><div className="text-xs text-gray-500 mt-1">Provider: {r.provider||'ai-proxy'}{r.model?` · ${r.model}`:''}</div></div><span className={`text-xs font-bold px-2 py-1 rounded-full ${r.status==='completed'?'bg-green-50 text-green-700':'bg-red-50 text-red-700'}`}>{r.status}</span></div>{r.status==='failed'?<div className="mt-4 rounded-lg bg-red-50 border border-red-100 p-3 text-sm text-red-700">This research run failed. Retry Deep Research to generate a new report.</div>:<><p className="text-sm text-gray-700 mt-4">{x.summary||'UNKNOWN'}</p><div className="grid grid-cols-2 md:grid-cols-3 gap-2 mt-4">{[['Profile Health',score(x.profile_health?.score),badge(x.profile_health?.status)],['Audience Authenticity',score(x.audience_authenticity?.score),`${badge(x.audience_authenticity?.status)} · confidence ${confidence(x.audience_authenticity?.confidence)}`],['Bot Risk',score(x.bot_risk?.score),`${badge(x.bot_risk?.status)} · confidence ${confidence(x.bot_risk?.confidence)}`],['Identity Consistency',score(x.identity_consistency?.score),badge(x.identity_consistency?.status)],['Activity',badge(x.activity?.status),'evidence-based'],['Platform Consistency',badge(x.platform_consistency?.status),'cross-source assessment']].map(([l,v,s])=><div key={String(l)} className="rounded-lg bg-gray-50 p-3"><div className="text-[10px] text-gray-400">{l}</div><div className="text-sm font-bold mt-1 capitalize">{v}</div><div className="text-[10px] text-gray-500 mt-1 capitalize">{s}</div></div>)}</div><div className="mt-4 rounded-xl border border-indigo-100 bg-indigo-50/50 p-3"><div className="text-[10px] uppercase tracking-wide text-indigo-600">Recommendation</div><div className="font-bold mt-1 capitalize">{badge(x.recommendation)}</div><p className="text-sm mt-1">{x.recommendation_reason||'UNKNOWN'}</p></div><Evidence title="Positive Signals" items={items(x.positive_signals)}/><Evidence title="Risk Signals" items={items(x.risk_signals)}/><Evidence title="Verified Facts" items={items(x.verified_facts)} classification="VERIFIED FACT"/><Evidence title="Observed Public Data" items={items(x.observed_data)} classification="OBSERVED PUBLIC DATA"/><Evidence title="Calculated Metrics" items={items(x.calculated_metrics)} classification="CALCULATED"/><Evidence title="Unknowns" items={items(x.unknowns)} classification="UNKNOWN"/><Evidence title="Limitations" items={items(x.limitations)}/><div className="mt-4"><h5 className="font-semibold mb-2">Sources</h5>{(r.search_evidence||[]).length===0?<p className="text-sm text-gray-400">No public search sources were returned.</p>:<div className="space-y-2">{(r.search_evidence||[]).map((s,i)=><div key={`${s.link}-${i}`} className="rounded-lg border border-gray-100 p-3"><a href={s.link} target="_blank" rel="noreferrer" className="text-sm font-semibold hover:underline break-words">{s.title||s.link}</a><div className="text-[10px] text-gray-400 mt-1">{s.source} · query: {s.query}</div><p className="text-xs text-gray-500 mt-1">{s.snippet||'No snippet returned.'}</p></div>)}</div>}</div></>}</div>}
function Evidence({title,items,classification}:{title:string;items:any[];classification?:string}){return <div className="mt-4"><div className="flex items-center gap-2 mb-2"><h5 className="font-semibold">{title}</h5>{classification&&<span className="text-[10px] px-2 py-0.5 rounded-full bg-gray-100 text-gray-500">{classification}</span>}</div>{items.length===0?<p className="text-sm text-gray-400">UNKNOWN</p>:<ul className="space-y-1">{items.slice(0,20).map((item:any,i)=><li key={i} className="text-sm text-gray-700">• {typeof item==='object'?String(item.detail||JSON.stringify(item)):String(item)}</li>)}</ul>}</div>}
function Info({label,value}:{label:string;value:string}){return <div className="border border-gray-100 rounded-xl p-3"><div className="text-xs text-gray-400">{label}</div><div className="text-sm font-medium mt-1">{value}</div></div>}
